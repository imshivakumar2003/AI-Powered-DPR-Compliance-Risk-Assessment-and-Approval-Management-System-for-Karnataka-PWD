import type { Metadata } from "next";
import "./globals.css";
import { Sidebar } from "@/components/layout/Sidebar";
import { Topbar } from "@/components/layout/Topbar";

export const metadata: Metadata = {
  title: "DPR-AI | MDoNER Smart Assessment Portal",
  description:
    "AI-powered Detailed Project Report quality assessment and risk prediction platform for the Ministry of Development of North Eastern Region.",
  keywords: "MDoNER, DPR, AI assessment, North East India, project management",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <div className="app-layout">
          <Sidebar />
          <div className="main-content">
            {children}
          </div>
        </div>
      </body>
    </html>
  );
}
